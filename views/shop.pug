<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= pageTitle %></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/products.css">
</head>
<body>
    <header class="main_header">
        <nav class="main_header-nav">
            <ul class="main_header-list">
                <li class="main_header-item"><a  href="/"></a></li>
                <li class="main_header-item"><a href="/admin/add-product"></a></li>
            </ul>
        </nav>
    </header>
    <main>
        <form action="/admin/product" method="POST" class="forms">
            <input type="text" class="input_product" name="title" placeholder="input name"/>
            <button type="submit" class="button">add product</button>
        </form>
    </main>
</body>
</html>extends layouts/main-layouts.pug 

block styles 

    link(rel="stylesheet", href="/css/products.css")

block content 
        main
            if prods.length > 0
                each product in  prods
                    article.card.product-item
                        h1.product__title #{product.title}
                        h2.product__price $20
            else 
                h2 no product